# Pruebas del backend

Las pruebas están separadas por comportamiento o módulo. Deben usar datos temporales, mocks para navegadores y servicios externos, y no leer credenciales ni modificar `storage/` real.
