# Esquemas de datos

Los archivos de esta carpeta definen los contratos Pydantic. Se separan por dominio (`bot`, `weekly_auto`, `execution`, `ai` y `recorder`) para que las rutas validen datos antes de iniciar tareas, automatizaciones o servicios.
