# Configuración

`settings.py` declara y valida todas las variables de entorno usadas por el backend. Las nuevas opciones deben añadirse allí y documentarse en `.env.example`; no se deben dispersar lecturas de `os.environ` en los servicios.
